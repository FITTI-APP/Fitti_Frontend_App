import 'package:flutter/material.dart';

class MyDietCalendar extends StatelessWidget {
  const MyDietCalendar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("MyDietCalendar"),
    );
  }
}
