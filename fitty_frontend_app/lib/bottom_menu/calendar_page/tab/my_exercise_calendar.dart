import 'package:flutter/cupertino.dart';

class MyExerciseCalendar extends StatelessWidget {
  const MyExerciseCalendar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("MyExerciseCalender"),
    );
  }
}
