import 'package:flutter/material.dart';

class MyDietHomeTab extends StatelessWidget {
  const MyDietHomeTab({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.directions_transit);
  }
}
